import type { ReactNode } from "react"

export function Grid2({ children }: { children: ReactNode }) {
  return <div className="grid2">{children}</div>
}

export function Grid3({ children }: { children: ReactNode }) {
  return <div className="grid3">{children}</div>
}

export function Grid4({ children }: { children: ReactNode }) {
  return <div className="grid4">{children}</div>
}
